export const metadata = {
  title: "2025-02-14 Title",
  description: `This is a long and descriptive description for testing purposes in the year 2025 on February 14th. It is important to make the text long and detailed to ensure it is properly tested and displayed on the website. Please do not remove this description as it is crucial for the testing process.`,
  date: "2025-02-14T15:00:00.000Z",
  keywords: ["nextjs", "cloudflare", "pages"],
};

## Blog : host a `nextjs` app on `cloudflare pages` 

## `wrangler.json`
```json
{
  "name": "nextjs-intl-cloudflare",
  "compatibility_date": "2024-09-23",
  "compatibility_flags": ["nodejs_compat"],
  "pages_build_output_dir": ".vercel/output/static"
}
```

## test
```ts
const a = 1;
const b = 2;
const c = a + b;

console.log('hello') 
console.log('world') 
```

## `build`
- when use `pnpm` change the `build` command on dashboard
- https://github.com/cloudflare/next-on-pages/issues/544#issuecomment-1813349018

```bash
# npx @cloudflare/next-on-pages@1

pnpm dlx @cloudflare/next-on-pages
```

## setup `env`
- `.dev.vars`
```txt
# [!code filename:.env.local]
API_KEY='API_KEY_DEV'

NEXT_PUBLIC_SITE_URL='http://localhost:8788'
```

- use
```bash
process.env.API_KEY
```

## bug: cant set response headers

Here's a comparison table between Cloudflare's Free and Pro plans:


## math

This sentence uses $\` and \`$ delimiters to show math inline: $`\sqrt{3x-1}+(1+x)^2`$
